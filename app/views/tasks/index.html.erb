<%= render "devise/shared/header" %>

<% if user_signed_in? %>
  <p><%= current_user.name %>さん、ようこそ</p>
<% end %>

<div class="task-btn">
<div class="btn btn-warning btn-lg">
  <%= link_to new_task_path  do %>
  <span class="task-text">投稿する<i class="fas fa-pen"></i></span>
  <% end %>
</div>  
</div>